# ============================================================================
# ~/.ssh/config.local — NOT committed.
# Copy to ~/.ssh/config.local and edit for your accounts/hosts.
#
# Run once to create the socket dir used by ControlPath:
#   mkdir -p ~/.ssh/sockets && chmod 700 ~/.ssh/sockets
# ============================================================================

# ----------------------------------------------------------------------------
#  Cluster Number 1
# ----------------------------------------------------------------------------
Host cluster1
	HostName hostname

Host cluster1*
	User username
	IdentitiesOnly yes
	IdentityFile ~/.ssh/xx_key

# ----------------------------------------------------------------------------
# Kerberos/GSSAPI Oriented Cluster
# ----------------------------------------------------------------------------
Host cluster2
	HostName hostname

Host cluster2*
	User                      username
	GSSAPIClientIdentity      username@client
	GSSAPIAuthentication      yes
	GSSAPIDelegateCredentials yes
	GSSAPITrustDns            yes
	CheckHostIP               no
	UserKnownHostsFile        ~/.ssh/known_hosts.local ~/.ssh/known_hosts
